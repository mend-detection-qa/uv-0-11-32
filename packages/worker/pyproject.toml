[project]
name = "worker"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "celery>=5.3.0",
    "api",
]

[tool.uv.sources]
api = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
